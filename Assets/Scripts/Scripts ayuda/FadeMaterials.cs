using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class FadeMaterials : MonoBehaviour {
    /*

	//Functions to Fade In and Out
	//May be useful change the "time" value
	public void FadeOut() {
		
		iTween.ValueTo(gameObject, iTween.Hash(
			"from", 1f, "to", 0f,
			"time", 1f, "easetype", "linear",
			"onupdate", "setAlpha"));
		               
		               }
		               
	public void FadeIn() {
			
		iTween.ValueTo(gameObject, iTween.Hash(
			"from", 0f, "to", 1f,
			"time", 1f, "easetype", "linear",
			"onupdate", "setAlpha"));
			               
			               }
	//Check if its an Image or a Text. 
	//More typs can be addded later
    public void setAlpha(float newAlpha) {
			
		if (GetComponent<Image> () != null) {
			Image mObj = this.GetComponent<Image> ();
			mObj.color = new Color(
				mObj.color.r, mObj.color.g, 
				mObj.color.b, newAlpha);
		} else {
			Text mObj = this.GetComponent<Text> ();
			mObj.color = new Color(
				mObj.color.r, mObj.color.g, 
				mObj.color.b, newAlpha);
		}
			
			
				
			}
			*/
		
}
