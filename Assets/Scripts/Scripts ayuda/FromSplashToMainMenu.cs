using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class FromSplashToMainMenu : MonoBehaviour {
    /*
	//Transition from Splash to Main menu automatically, putting a Fade In and a Fade out
	// Use this for initialization
	void Start () {
		 
		StartCoroutine (ChangeToMainMenu ());
	}
	


	IEnumerator ChangeToMainMenu()
	{

		this.GetComponent<FadeMaterials> ().FadeIn ();

		yield return new WaitForSeconds (3f); // This time and the time below may change depending on the time
		// you want the logo to be active
		this.GetComponent<FadeMaterials> ().FadeOut ();
		yield return new WaitForSeconds (3f);
		Application.LoadLevel ("MainMenu");
	}*/
}
