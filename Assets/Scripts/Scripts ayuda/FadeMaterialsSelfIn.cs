using UnityEngine;
using System.Collections;

public class FadeMaterialsSelfIn : MonoBehaviour {
    /*
	// Use this for initialization
	void Start () {
		//This script always has to be attached with a FadeMaterials
		//Otherwise, doesn't works.
		//Automatically put a FadeIn in the object
		this.GetComponent<FadeMaterials> ().FadeIn ();
	}
	
	// Update is called once per frame
	void Update () {
	
	}*/
}
